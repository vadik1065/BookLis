import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.booklis.app",
  appName: "booklis",
  webDir: "../BookLis/.next/server/pages",
  bundledWebRuntime: false,
};

export default config;
